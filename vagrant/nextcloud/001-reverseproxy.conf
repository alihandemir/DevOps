# Allgemeine Proxy Einstellungen
ProxyRequests Off
<Proxy *>
      Order deny,allow
      Allow from all
</Proxy>

# Weiterleitungen cloud
ProxyPass /cloud http://cloud
ProxyPassReverse /cloud http://cloud
